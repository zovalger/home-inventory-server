import { CreateProductTransactionPipe } from './create-product-transaction.pipe';

describe('CreateProductTransactionPipe', () => {
  it('should be defined', () => {
    expect(new CreateProductTransactionPipe()).toBeDefined();
  });
});
